const getSearchedData = async searchedValue => {
  return ["Searching...", searchedValue]
}
const generateSearchedItem = item => {
  return item
}

export{
  getSearchedData,
  generateSearchedItem,
}